 <h1>Edit Task</h1>

<div class="tasks-li">

<%= form_for(@task) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :details %>
  <%= form.text_field :details %>

  <%= form.submit %>
<% end %>
  <p><%= link_to "Back to list", tasks_path%></p>

</div>
